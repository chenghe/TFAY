<?xml version="1.0" encoding="UTF-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 设置背景色（白色） -->

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />

            <gradient
                android:endColor="@color/mx_circle_item_vote_progress_bg"
                android:startColor="@color/mx_circle_item_vote_progress_bg" />
        </shape>
    </item>

    <!-- 设置进度条颜色（蓝色） -->

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />

                <gradient
                    android:endColor="@color/mx_blue_viewbg"
                    android:startColor="@color/mx_blue_viewbg" />
            </shape>
        </clip>
    </item>

</layer-list>